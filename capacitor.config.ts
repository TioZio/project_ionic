import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mprograming_tiozio',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
